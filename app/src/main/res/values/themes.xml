<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme con Material Components -->
    <style name="Base.Theme.ActividadesDiarias" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Colores personalizados -->
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorSecondary">@color/mint_secondary</item>
        <item name="android:colorBackground">@color/background_light</item>
    </style>

    <style name="Theme.ActividadesDiarias" parent="Base.Theme.ActividadesDiarias" />
</resources>